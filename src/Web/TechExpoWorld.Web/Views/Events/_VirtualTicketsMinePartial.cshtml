@using TechExpoWorld.Web.ViewModels.Events
@model IEnumerable<TicketMineViewModel>

<table class="table table-hover">
    <thead class="bg-info text-white">
        <tr>
            <th scope="col">#</th>
            <th scope="col">Event</th>
            <th scope="col" class="text-end">Virtual Tickets</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var virtualTicketMine in Model)
        {
            <tr>
                <th scope="row">@(ViewBag.CountVirtualTickets++)</th>
                <td>@virtualTicketMine.EventTitle</td>
                <td>
                    <a asp-controller="Events" asp-action="CancelTicket" asp-route-id="@virtualTicketMine.EventId" asp-route-ticketId="@virtualTicketMine.TicketId">
                        <span class="btn btn-secondary btn-outline-dark text-white float-end">Cancel this Ticket</span>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>